import { DEFAULT_THEME } from 'app/constants/main';

export default {
  // external params
  baseUrl: null,
  apiBase: null,
  siteId: null,
  theme: DEFAULT_THEME,

  // local state
  loadingCounter: 0,
};
